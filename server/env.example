PORT=5000
CLIENT_URL=http://localhost:5173

# After `npm run build`, serve the React app from this server
# SERVE_STATIC=true

# Contact email is handled by EmailJS in the client — see client/.env.example
